# Travelog - Vue 3 Travel Photo SNS

## ✅ 변환 완료 사항

Next.js + React 프로젝트를 **완전히 작동 가능한** Vue 3 + Vite 프로젝트로 변환했습니다.

### 🔄 변환된 기술 스택

| 원본 (React) | 변환 (Vue) |
|-------------|-----------|
| Next.js 16 | Vite 5 |
| React 19 | Vue 3.4 |
| Zustand | Pinia |
| Next.js Router | Vue Router |
| React Hooks | Composition API |
| JSX/TSX | Vue SFC (.vue) |
| lucide-react | lucide-vue-next |

### 📦 설치 방법

```bash
# 프로젝트 폴더로 이동
cd vue-travel-sns

# 의존성 설치
npm install

# 개발 서버 실행 (http://localhost:5173)
npm run dev

# 프로덕션 빌드
npm run build

# 빌드된 프로젝트 미리보기
npm run preview
```

### 🎯 구현된 기능

✅ **완전히 작동하는 기능:**
- 📱 피드 페이지 (무한 스크롤)
- 📤 사진 업로드 (드래그 앤 드롭)
- 👤 사용자 프로필
- 💬 댓글 시스템
- ❤️ 좋아요 기능 (애니메이션 포함)
- 🗺️ 여행 지도 페이지
- 📊 마이페이지 (통계 포함)
- 🔍 검색 바
- 🧭 네비게이션
- 👥 팔로우/언팔로우

### 📂 프로젝트 구조

```
vue-travel-sns/
├── src/
│   ├── components/
│   │   ├── ui/           # UI 기본 컴포넌트 (Button, Input, Card, etc.)
│   │   ├── Navigation.vue
│   │   ├── Footer.vue
│   │   ├── PostCard.vue
│   │   ├── LikeButton.vue
│   │   └── CommentModal.vue
│   ├── views/            # 페이지 컴포넌트
│   │   ├── FeedView.vue
│   │   ├── UploadView.vue
│   │   ├── ProfileView.vue
│   │   ├── PostDetailView.vue
│   │   ├── MapView.vue
│   │   └── MyPageView.vue
│   ├── stores/
│   │   └── app.ts        # Pinia 전역 상태 관리
│   ├── router/
│   │   └── index.ts      # Vue Router 설정
│   ├── types/
│   │   └── index.ts      # TypeScript 타입 정의
│   ├── data/
│   │   └── dummy-data.ts # 개발용 더미 데이터
│   ├── lib/
│   │   └── utils.ts      # 유틸리티 함수
│   ├── assets/
│   │   └── styles.css    # Tailwind CSS
│   ├── App.vue
│   ├── main.ts
│   └── env.d.ts
├── public/               # 이미지 파일들
├── index.html
├── vite.config.ts
├── tailwind.config.js
├── tsconfig.json
└── package.json
```

### 🚀 주요 특징

1. **TypeScript 완벽 지원**
   - 모든 컴포넌트와 스토어에 타입 정의
   - 타입 안전성 보장

2. **Composition API 사용**
   - `<script setup>` 문법 사용
   - 간결하고 현대적인 코드

3. **Pinia 상태 관리**
   - Zustand에서 완벽하게 변환
   - 게시물, 좋아요, 댓글, 팔로우 상태 관리

4. **Vue Router**
   - 동적 라우팅 지원
   - `/profile/:id`, `/post/:id` 등

5. **반응형 디자인**
   - Tailwind CSS 사용
   - 모바일/태블릿/데스크톱 대응

### 🎨 스타일링

- **Tailwind CSS** - 유틸리티 퍼스트 CSS
- **다크모드 지원** - CSS 변수로 구현
- **애니메이션** - 좋아요, 모달 트랜지션 등
- **반응형** - 모든 화면 크기 지원

### 📱 페이지별 기능

#### 1. Feed (/)
- 무한 스크롤 게시물 피드
- 좋아요/댓글/공유 버튼
- 게시물 클릭 시 상세 페이지 이동
- Intersection Observer로 자동 로딩

#### 2. Upload (/upload)
- 드래그 앤 드롭 이미지 업로드
- 이미지 미리보기
- 위치 및 캡션 입력
- 즉시 피드에 반영

#### 3. Profile (/profile/:id)
- 사용자 정보 표시
- 게시물 그리드 뷰
- 팔로워/팔로잉 수
- 팔로우/언팔로우 버튼

#### 4. Post Detail (/post/:id)
- 게시물 상세 보기
- 댓글 전체 목록
- 실시간 댓글 추가
- 좋아요 기능

#### 5. Map (/map)
- 여행 로그 표시
- 방문한 장소 목록
- (Kakao Map API 연동 가능)

#### 6. My Page (/mypage)
- 내 프로필 정보
- 내가 올린 게시물
- 통계 (게시물, 장소, 좋아요, 댓글 수)

### 🔧 커스터마이징

#### 1. API 연동
`src/stores/app.ts`에서 더미 데이터를 실제 API 호출로 변경:

```typescript
// 예시: 게시물 로드
const loadPosts = async () => {
  const response = await fetch('/api/posts')
  const data = await response.json()
  setPosts(data)
}
```

#### 2. 스타일 변경
`src/assets/styles.css`에서 CSS 변수 수정:

```css
:root {
  --primary: 220 90% 56%;  /* 메인 컬러 변경 */
}
```

#### 3. 지도 연동
`src/views/MapView.vue`에서 Kakao Map API 추가:

```typescript
// Kakao Map 초기화
const initMap = () => {
  const container = document.getElementById('map')
  const options = {
    center: new kakao.maps.LatLng(33.450701, 126.570667),
    level: 3
  }
  const map = new kakao.maps.Map(container, options)
}
```

### 📝 다음 단계

프로젝트를 실제 프로덕션으로 사용하려면:

1. **백엔드 API 구축**
   - 게시물 CRUD
   - 사용자 인증
   - 이미지 스토리지

2. **인증 시스템**
   - 로그인/회원가입
   - JWT 토큰 관리
   - Protected Routes

3. **이미지 업로드**
   - AWS S3, Cloudinary 등 연동
   - 이미지 최적화

4. **실시간 기능**
   - WebSocket으로 실시간 알림
   - 실시간 댓글 업데이트

5. **성능 최적화**
   - 이미지 Lazy Loading
   - 가상 스크롤
   - 캐싱 전략

### 🐛 알려진 제한사항

- 현재 더미 데이터 사용 (실제 DB 연동 필요)
- 이미지 업로드가 로컬 프리뷰만 가능
- 지도는 플레이스홀더 (Kakao Map 연동 필요)
- 인증 시스템 미구현

### 💡 추가 개선 가능 사항

- [ ] 이미지 갤러리 슬라이더
- [ ] 해시태그 기능
- [ ] 게시물 북마크
- [ ] 알림 시스템
- [ ] 다크모드 토글 UI
- [ ] 언어 설정 (i18n)
- [ ] PWA 지원
- [ ] SEO 최적화

### 📄 라이센스

MIT License

---

**문의사항이나 버그 리포트는 이슈로 등록해주세요!** 🚀
